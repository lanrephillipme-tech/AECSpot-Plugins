<Window x:Class="AECSpot.FindFiltersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="AEC Spot - Find Filters"
        Width="900"
        Height="600"
        MinWidth="720"
        MinHeight="480"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="#F7F7F7">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/AECSpot;component/Dictionaries/ApplicationDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="42" />
            <RowDefinition Height="*" />
            <RowDefinition Height="58" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource MainBlue}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid Margin="14,0,8,0">
                <TextBlock Text="Find Filters" Foreground="White" FontSize="15" VerticalAlignment="Center" />
                <Button HorizontalAlignment="Right" Style="{StaticResource TextButton}" Content="Close" Foreground="White" Click="CloseButton_Click" />
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="{StaticResource MainLightGrey}" BorderThickness="1" CornerRadius="4" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBox Grid.Row="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="7" ToolTip="Search filter names or categories" />
                    <Grid Grid.Row="2">
                        <ListBox ItemsSource="{Binding FilterItems}" SelectedItem="{Binding SelectedFilter, Mode=TwoWay}" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="4">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding UsageSummary}" Foreground="{StaticResource MainDarkGrey}" FontSize="11" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <TextBlock Text="No parameter filters found in this project."
                                   HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"
                                   Foreground="{StaticResource MainDarkGrey}" TextWrapping="Wrap"
                                   Visibility="Collapsed">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasFilters}" Value="False"><Setter Property="Visibility" Value="Visible" /></DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Column="2" BorderBrush="{StaticResource MainLightGrey}" BorderThickness="1" CornerRadius="4" Padding="18">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel DataContext="{Binding SelectedFilter}">
                        <TextBlock Text="Filter Information" FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource MainDarkGrey}" />
                        <TextBlock Text="{Binding Name}" FontSize="22" Margin="0,6,0,4" TextWrapping="Wrap" />
                        <TextBlock Text="{Binding UsageSummary}" Foreground="{StaticResource MainDarkGrey}" Margin="0,0,0,16" />

                        <TextBlock Text="Categories" FontWeight="SemiBold" Foreground="{StaticResource MainDarkGrey}" />
                        <TextBlock Text="{Binding CategoriesSummary}" TextWrapping="Wrap" Margin="0,4,0,14" />

                        <TextBlock Text="Rule Summary" FontWeight="SemiBold" Foreground="{StaticResource MainDarkGrey}" />
                        <TextBlock Text="{Binding RuleSummary}" TextWrapping="Wrap" Margin="0,4,0,14" />

                        <TextBlock Text="Used in Views" FontWeight="SemiBold" Foreground="{StaticResource MainDarkGrey}" />
                        <ItemsControl ItemsSource="{Binding ViewUsages}" Margin="0,4,0,14">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ViewName}" Margin="0,2" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="No project views use this filter." Foreground="{StaticResource MainDarkGrey}" Margin="0,4,0,14">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ViewUsageCount}" Value="0"><Setter Property="Visibility" Value="Visible" /></DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <TextBlock Text="Used in View Templates" FontWeight="SemiBold" Foreground="{StaticResource MainDarkGrey}" />
                        <ItemsControl ItemsSource="{Binding TemplateUsages}" Margin="0,4,0,8">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ViewName}" Margin="0,2" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="No view templates use this filter." Foreground="{StaticResource MainDarkGrey}" Margin="0,4,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TemplateUsageCount}" Value="0"><Setter Property="Visibility" Value="Visible" /></DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource MainSoftLightGrey}">
            <Button Content="Close" Style="{StaticResource BlueButtonStyle}" HorizontalAlignment="Right" Margin="0,8,16,8" Click="CloseButton_Click" />
        </Border>
    </Grid>
</Window>


